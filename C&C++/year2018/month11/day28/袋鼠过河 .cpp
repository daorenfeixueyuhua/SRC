#include <iostream>
#include <vector>

using namespace::std;

int Dump(vector<int> & N){
	
}

int main(){

	int n;
	cin >> n;
	vector<int> N(n,0);
	for(int i=0;i<n;++i)
		cin >> N[i];
	 
	return 0;
} 
