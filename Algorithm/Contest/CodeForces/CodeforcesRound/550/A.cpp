#include <bits/stdc++.h>
using namespace std;

bool comp(const int a, const int b)
{
    return a < b ? 1 : 0;
}
// 1 出现连续字符，2 且只出现一次

int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        // 用于记录下连续字符出现的次数
        vector<int> times(26, 0);
        string msg = "Yes";
        string s;
        cin >> s;
        for(char c : s){
            
        }
    }
    return 0;
}