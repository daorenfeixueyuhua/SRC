#include <bits/stdc++.h>
using namespace std;
int main() {
    string s1, s2;
    cin >> s1 >> s2;
    vector<int> temp(s2.size() + 1, 0);
    vector<vector<int>> dp(s1.size(), temp);

    return 0;
}